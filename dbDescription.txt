dispute
    id: string
    user1_id: string
    user2_id: string
    description: string
    amount: int
    date: dateTime
    status: enum(CREATED, ACCEPTED, REJECTED, PENDING, TIE, WIN1, WIN2, REPORTED)

deposit
    id: string
    user_id: string
    dispute_id: string
    method: int
    payment_link: str
    status: enum(LINK_SENT, SUCCESS)

payout
    id: string
    disput_id: string
    method: int
    data: string
    status: enum(CREATED, SUCCESS)

proof
    id: string
    description: string
    user_id: string
    dispute_id: string
    date: dateTime
